if [ -d "{{ ansible_user_dir }}/{{ bash_profile_d }}" ]; then
    for file in $(ls "{{ ansible_user_dir }}/{{ bash_profile_d }}"); do
        if [ -r "{{ ansible_user_dir }}/{{ bash_profile_d }}/$file" ]; then
            . "{{ ansible_user_dir }}/{{ bash_profile_d }}/$file"
        fi
    done
fi
